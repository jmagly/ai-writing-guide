# Documentation Sync Report — 2026-07-21

## Scope

- Direction: `code-to-docs`
- Release baseline: `v2026.7.13..b5f814e6761198f30cc3357487d848051b6911c8`
- Required lanes: `agentic/code/**`, repository Markdown, CLI/config schema,
  release notes, provider/model routing, Fortemi integration, and repository
  identity/signing policy
- Mode: release-flow bounded audit with dry review before edits

## Findings

| Area                             | Evidence                                                                                                                                                            | Result       |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| Provider/model routing           | CLI reference, model configuration, provider decision record, conformance docs, and Unreleased changelog cover the shipped model policy/discovery/wrapper surfaces. | Synchronized |
| Fortemi Core and Knowledge Shard | Locked `@fortemi/core@2026.7.9`, immutable `core-v1` receipt, ADR, integration guide, agent contract, and blocking conformance workflow agree.                      | Synchronized |
| Repository identity and signing  | Changelog, security/release docs, local project config, and delivery policy distinguish Gitea transport, commit signing, and release signing.                       | Synchronized |
| Project config reference         | Runtime/schema added `remotes.transport` and `delivery.release_signing`, but the field reference did not enumerate their shapes or safety constraints.              | Fixed        |

## Changes Applied

- Expanded `docs/configuration/aiwg-config.md` with:
  - `delivery.committer`, `delivery.signing`, and
    `delivery.release_signing` fields;
  - separate commit/release signing example and secret-handling boundary;
  - `remotes.tracker_actor` and `remotes.transport` fields;
  - tracker-write versus Git-transport identity example and accepted enums.

## Validation

- `npx prettier --check docs/configuration/aiwg-config.md`
- `npm test -- test/unit/config/aiwg-config.test.ts test/unit/config/get-set-project.test.ts test/unit/rules/delivery-policy-preflight.test.ts test/unit/skills/routing-docs.test.ts`
  - 4 files passed
  - 120 tests passed
- `npm run typecheck`
- `git diff --check`

## Human Review

No unresolved documentation conflicts. Historical release announcements and
planning records retain the package/version facts that were true when written.
The release flow should next create the `2026.7.14` changelog section and stable
announcement from the current Unreleased material.

## Handoff

Proceed to the `flow-release` stable `aiwg-npm` plan for `2026.7.14` after the
doc-sync commit is signed, pushed, and exact-head CI is green.
