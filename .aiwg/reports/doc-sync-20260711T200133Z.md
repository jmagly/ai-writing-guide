# Doc Sync Report — 2026-07-11 Release Prep

**Timestamp:** 2026-07-11T20:01:33Z  
**Direction:** code-to-docs  
**Scope:** post-`v2026.7.11` delta, internal `.aiwg` design docs, external docs
for Cockpit/serve/agent-loop users, and next release artifacts.  
**Release action:** prepared `2026.7.12` artifacts only; no tag or publish step
was run.

## Inputs

- `git describe --tags --abbrev=0` → `v2026.7.11`
- `git log --oneline --max-count=18`
- `git diff --name-only v2026.7.11..HEAD`
- AIWG discovery: `aiwg discover "doc sync code to docs release documentation"`
- AIWG skill body: `aiwg show skill doc-sync`

## Findings

1. Cockpit gained stale Docker/container agent recovery through the Bridge and
   UI, but the operator docs still focused on launch, attach, and live matrix
   verification.
2. Internal instance-control ADRs described transport/session surfaces but not
   the new `POST /api/instances/:id/reconnect` recovery extension.
3. The external release docs had no `v2026.7.12` prepared announcement and the
   changelog `Unreleased` section was empty.
4. The base and Cockpit package versions were still `2026.7.11`; the Cockpit
   lockfile still carried `2026.7.10`.
5. The LFD remediation batch was implemented and covered by CLI/manual-provider
   docs, but the release artifacts did not yet tell operators what changed.

## Fixes Applied

- Updated `apps/cockpit/README.md` with the stale-agent recovery workflow:
  `agent unreachable`, **Reconnect**, executor-first recovery, Docker
  `agent-reconnect` fallback, and host-daemon launch guidance.
- Updated `docs/serve-guide.md` with the published Bridge recovery endpoint and
  user-facing recovery semantics.
- Updated internal Cockpit architecture docs:
  - `.aiwg/architecture/cockpit-instance-control-interface.md`
  - `.aiwg/architecture/adr-cockpit-session-attach-model.md`
  - `.aiwg/architecture/adr-cockpit-instance-control-substrate.md`
- Prepared release artifacts:
  - bumped base package and lockfile to `2026.7.12`
  - bumped Cockpit package and lockfile to `2026.7.12`
  - bumped `.claude-plugin/marketplace.json` metadata to `2026.7.12`
  - added `docs/releases/v2026.7.12-announcement.md`
  - added the release to `docs/releases/_manifest.json`
  - added `CHANGELOG.md` release notes
- Recorded this doc-sync report and refreshed
  `.aiwg/reports/doc-sync-last-run.json`.

## User-Facing Coverage

Users now have a clear path to take advantage of the new Cockpit features:

- launch a real Cockpit stack with the optional host daemon;
- read transport and host-daemon posture in Inventory;
- identify stale running containers via `agent unreachable`;
- use **Reconnect** instead of destroying or recreating a running container;
- return to Sessions after recovery and attach through the normal
  observer-default workflow;
- use the manual provider test runbook when validating LFD controls against a
  real provider CLI.

## Release Gate Notes

- Release artifacts are prepared for `2026.7.12`.
- The release was not cut: no tag was created and no npm publish command was
  run.
- Before cutting the release, complete the required Cockpit live matrix
  (`host,container,vm`) and attach current UAT evidence.

## Validation

- `npm run check:versions` — pass
- `npm run docs:collect:dry` — pass; manifest already up to date
- `npm --prefix apps/cockpit run test:web` — pass, 8 files / 78 tests
- `git diff --check` — pass

Known test noise: the Cockpit web suite still prints the jsdom
`HTMLCanvasElement.prototype.getContext` warning from xterm import; this is the
same non-failing warning seen in prior runs.
