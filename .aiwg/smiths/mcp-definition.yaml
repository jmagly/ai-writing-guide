# MCP Environment Definition
# Generated by /smith-mcpdef
# Last updated: 2025-12-13
#
# This file describes the local environment's capability to run
# MCPSmith-generated Docker containers with MCP servers.

docker:
  available: true
  version: "29.0.1"
  daemon_running: true

node:
  available: true
  version: "22.19.0"
  npm_version: "11.7.0"

mcp:
  sdk_version: "1.24.3"
  spec_version: "2025-11-25"
  transports:
    - stdio

base_images:
  node_alpine:
    image: "node:20-alpine"
    cached: true
  node_slim:
    image: "node:20-slim"
    cached: false

network:
  name: "aiwg-mcp-network"
  exists: false

ports:
  range_start: 9100
  range_end: 9199

# Capability summary
capabilities:
  can_build_containers: true
  can_run_mcp_servers: true
