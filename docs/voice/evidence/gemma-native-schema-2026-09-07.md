# Gemma native structured-output follow-up

Twelve new calls completed using Gemma4 31B Q4_K_M and unchanged source, exemplar, system and user prompt bytes from the twelve prior edit cells. Native response_format JSON schema was the intervention. All frozen input hashes verified after generation.

| Outcome | Calls |
|---|---:|
| Normal completion | 12 |
| Valid bounded proposal | 6 |
| Rejected for exceeding 80 source words in an edit | 6 |
| Factual fidelity pass | 2 |
| Joint factual fidelity and voice pass | 0 |

The two fidelity passes were Twain/equipment-library/paragraph-anchors and Twain/OMP/exact-span. Both remained generic exposition. Other valid candidates added a fragile-setup judgment, an unspecified standard comparison, a volunteer requirement in place of a request, or invented personal investigation. Darwin/equipment-library showed promising connected reasoning but failed fidelity. Invalid proposals were not semantically scored.

The primary session model reviewed all six applied candidates; this is development evidence, not independent reader qualification. Hash-bound production revision replay retained the original for all six reviewed candidates and all six no-review controls.

Native schema eliminated the previous fence-wrapper failures in these twelve new draws. It did not enforce source-word budgets or semantic fidelity. The earlier twelve failures remain in their original record. No response repair, replacement draws, or relaxed edit budgets were used.

Offline capture through Matric-eval and Inspect showed the typed ResponseSchema path omitted maxItems. The tested adapter sent the complete schema through extra_body.response_format, and offline capture verified the exact outgoing schema before generation. This finding concerns the installed versions and path only.

Fortemi was restored healthy and Gemma unloaded, confirmed by service-receipt.json. Detailed outcomes, candidate hashes, primary review and production replay are in proposal-results.json, primary-notes.json and runtime-results.json.

[Aggregate receipts](gemma-native-schema-2026-09-07.json). Private raw artifacts remain in `.aiwg/working/voice-2292/expression-native-gemma-01/`. See the [preceding current-model comparison](current-model-expression-2026-09-07.md) for the original twelve format failures. This follow-up does not qualify all channels or satisfy independent evaluation.
