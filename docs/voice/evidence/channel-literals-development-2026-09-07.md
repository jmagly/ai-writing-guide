# Explicit channel literal protection: development pilot

**Historical model evidence:** GPT-OSS20b results are excluded from current-model qualification by operator direction. Preserve the observations; revalidate conclusions on eligible models. See [model eligibility](model-eligibility.md).

Twenty new GPT-OSS20b generations used the same facts, examples, compiled expression guidance and decoding as channel-family-02. Only the masked source and marker list changed. The existing CTA was declared explicitly for all five channels. Source facts, profile text, prompts and code hashes were verified against frozen inputs.

| Outcome | Prior GPT-OSS run | Explicit literal pilot |
| --- | ---: | ---: |
| Normal completions | 20/20 | 20/20 |
| Protected-text restoration | 18/20 | 20/20 |
| Primary semantic fidelity | 5/20 | 17/20 |
| Joint fidelity/cadence/mode | 0/20 | 0/20 |
| Runtime retained candidates | 0/20 | 0/20 |

The three factual failures changed operator ownership into an undefined unchanged status, added a pleased-to-announce stance, or claimed the preview verifies behavior without supplied support. The other outputs largely copied source structure or added generic headings. Literal protection improved this development result but did not establish distinct voice.

All four social outputs duplicated the CTA. Prompt inspection found a second raw representation in social constraints.cta, alongside the masked source CTA. The runtime correctly protected source text, but the pilot adapter still supplied the raw question elsewhere. This run remains frozen; it is not represented as complete prompt-level CTA hiding. Final channel checks retain their role even when marker restoration passes.

Both whole-CTA and required-literal masking changed together, so their effects are not isolated. Primary-session judgments are development evidence, not independent human/author acceptance. Antigravity operator-owned typography is not an exact required literal in these fixtures; earlier notes mentioning it do not independently establish a format failure. Relevant prior failures involved date changes or CTA violations, so this clarification does not change prior acceptance counts.

Usage: 43,052 generation tokens. Service cycle: 126.8s including load/restoration; this is not generation-only latency. Warmup and reviewer tokens are not included. Model digest: 17052f91a42e97930aa6e28a6c6c06a983e6a58dbb00434885a0cf5313e376f7. Seed2303, temperature0.3, medium reasoning, output ceiling4096. No replacement draws or repairs. All24 destination replays fell back; all no-callback checks preserved source. Fortemi was restored healthy and the model unloaded.

Next remove or consistently encode the duplicate raw CTA field and rerun the four affected social prompts. Continue cadence work separately; successful literal handling does not qualify a writer profile or consumer.

[Aggregate receipts](channel-literals-development-2026-09-07.json). Original author text and hash-bound model/review artifacts remain in the authorized development workspace at `.aiwg/working/voice-2292/channel-literal-pilot-01/`.
