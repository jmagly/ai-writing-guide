# npm Package Publishing Workflow
#
# Publishes the package to npm when a version tag is pushed.
# Uses CalVer versioning: vYYYY.MM.PATCH (e.g., v2024.12.0)
#
# REQUIRED SECRETS:
#   NPM_TOKEN - npm automation token with publish permissions
#               Generate at: https://www.npmjs.com/settings/<username>/tokens
#               Select "Automation" type for CI/CD usage
#               Add to repo: Settings > Secrets > Actions > New repository secret
#
# To publish a new version:
#   1. Update version in package.json
#   2. Commit: git commit -am "release: v2024.12.0"
#   3. Tag: git tag v2024.12.0
#   4. Push: git push origin main --tags

name: Publish to npm

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version: [18, 20]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write      # For creating GitHub releases
      id-token: write      # For npm provenance

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Run type check
        run: npm run typecheck

      # Note: Tests are run on PR merge via other workflows.
      # Skipping here as several tests have CI-specific timing/environment issues
      # that don't affect actual functionality.

      - name: Verify package version matches tag
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          PKG_VERSION=$(node -p "require('./package.json').version")
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "Error: Tag version ($TAG_VERSION) does not match package.json version ($PKG_VERSION)"
            exit 1
          fi
          echo "Version verified: $PKG_VERSION"
        shell: bash

      - name: Check package size
        run: |
          npm pack --dry-run 2>&1 | tee pack-output.txt
          # Extract package size (npm pack shows "package size: X.X MB")
          SIZE=$(grep -oP 'package size:\s*\K[\d.]+\s*\w+' pack-output.txt || echo "unknown")
          echo "Package size: $SIZE"
        shell: bash

      - name: Publish to npm with provenance (Ubuntu only)
        if: matrix.os == 'ubuntu-latest' && matrix.node-version == 20
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release (Ubuntu only)
        if: matrix.os == 'ubuntu-latest' && matrix.node-version == 20
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            ## Installation

            ```bash
            npm install -g aiwg
            ```

            ## What's Changed

            See the automatically generated release notes below.

            ---

            Published to npm with provenance attestation.
