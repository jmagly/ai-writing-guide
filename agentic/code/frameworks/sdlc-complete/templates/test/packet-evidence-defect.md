# Packet Evidence Defect Attachment

- Defect ID:
- Requirement / test case IDs:
- Failing build ID:
- Reproducing environment and topology fingerprint:
- Evidence bundle / capture digest / recipe:
- Observed behavior with frame or context-bound stream citations:
- Expected behavior:
- DNS/TCP/TLS/HTTP, retry, reset, latency, or dependency-path observations:
- Missing traffic, encryption, clock, and analyst limitations:
- Comparison to known-good bundle: comparable / partially comparable / incomparable
- Normalized differences and excluded volatile fields:
- Fix build and post-fix evidence bundle:
- Verification result:

Keep payload, credentials, cookies, authorization headers, and unrelated traffic
out of the attachment. Raw capture inclusion and any provider transfer require
their own explicit policy decisions.
