# Conversion (#1539) — flow-inception-to-elaboration Step 6 authoring panel.
apiVersion: flow.aiwg.io/v1
kind: FlowCapability
metadata:
  name: i2e-author-use-cases
  labels: { domain: requirements }
spec:
  description: Author 10+ use-case specifications (top 3 = validated steel threads, remainder = Construction functional requirements) with actors, flows, postconditions, acceptance criteria, and SAD component mapping for traceability (flow Step 6 authoring agent).
  version: "1.0.0"
  inputs: []
  outputs:
    - { name: use_cases, type: array }
  agent: requirements-analyst
  idempotent: false
