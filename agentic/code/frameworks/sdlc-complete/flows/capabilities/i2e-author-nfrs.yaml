# Conversion (#1539) — flow-inception-to-elaboration Step 6 authoring panel.
apiVersion: flow.aiwg.io/v1
kind: FlowCapability
metadata:
  name: i2e-author-nfrs
  labels: { domain: requirements }
spec:
  description: Author the Supplemental Specification (non-functional requirements) — performance, scalability, availability, security, usability, maintainability targets — derived from the SAD quality attributes and baseline plan (flow Step 6 authoring agent).
  version: "1.0.0"
  inputs: []
  outputs:
    - { name: supplemental_spec, type: string }
  agent: requirements-analyst
  idempotent: false
