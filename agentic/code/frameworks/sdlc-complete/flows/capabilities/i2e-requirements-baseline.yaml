# Conversion (#1539) — flow-inception-to-elaboration Step 6 synthesis.
apiVersion: flow.aiwg.io/v1
kind: FlowCapability
metadata:
  name: i2e-requirements-baseline
  labels: { domain: requirements }
spec:
  description: Synthesize the use-case specs, supplemental specification, and Master Test Plan into the established requirements baseline, confirming use-case → SAD-component traceability across the set (flow Step 6 synthesis).
  version: "1.0.0"
  inputs: []
  outputs:
    - { name: requirements_baseline, type: object }
  agent: documentation-synthesizer
  idempotent: false
