# #1539 conversion of flow-iteration-dual-track Step 4 (Midpoint quality gate).
apiVersion: flow.aiwg.io/v1
kind: FlowCapability
metadata:
  name: dualtrack-midpoint-security
  labels: { domain: iteration }
spec:
  description: Run the security gate over Delivery work — code security scanning results, authentication/authorization implementation, data protection measures, security test coverage; emit PASS/FAIL/WARNING.
  version: "1.0.0"
  inputs: []
  outputs:
    - { name: security_gate, type: string }
  agent: security-gatekeeper
  idempotent: true
