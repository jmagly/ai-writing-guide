# #1539 conversion capability — flow-delivery-track Step 4 (test panel).
apiVersion: flow.aiwg.io/v1
kind: FlowCapability
metadata:
  name: delivery-test-unit
  labels: { domain: delivery }
spec:
  description: Run the full unit test suite plus regression and coverage analysis against the iteration's coverage target (flow Step 4 test panel).
  version: "1.0.0"
  inputs: []
  outputs:
    - { name: unit_results, type: object }
  agent: test-engineer
  idempotent: true
