# Conversion (#1539) — flow-inception-to-elaboration Step 2 planning panel.
apiVersion: flow.aiwg.io/v1
kind: FlowCapability
metadata:
  name: i2e-plan-steel-threads
  labels: { domain: phase-transition }
spec:
  description: Identify 2-3 architecturally significant steel-thread use cases that exercise key patterns, cover high-priority architectural risks, and demonstrate end-to-end flow (flow Step 2 planning agent).
  version: "1.0.0"
  inputs: []
  outputs:
    - { name: steel_thread_draft, type: string }
  agent: requirements-analyst
  idempotent: false
