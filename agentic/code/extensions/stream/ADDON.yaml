apiVersion: ops.aiwg.io/v1
kind: OpsExtension
metadata:
  name: stream
  labels:
    domain: stream-operations
spec:
  extends: ops-complete
  description: "Streaming operations — transcoders, restreaming, platform integrations"
  version: "1.0.0"
  capabilities: auto-discover
  kinds:
    - name: StreamEncoderConfig
      schema: schemas/stream-artifacts.schema.json
    - name: StreamPipeline
      schema: schemas/stream-artifacts.schema.json
