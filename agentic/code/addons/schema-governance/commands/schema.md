---
description: Guide schema discovery, authoring, validation, compatibility, and projections
argument-hint: "<need or aiwg schema action>"
allowed-tools: Read, Write, Bash
---

Interpret plain-language requests through `schema-intake`; do not require the
operator to know schema terminology. For explicit operations, use the core
`aiwg schema` runtime. Route new contracts through `schema-lifecycle`, changes
through `schema-change`, and finish with independent `schema-review` evidence.
