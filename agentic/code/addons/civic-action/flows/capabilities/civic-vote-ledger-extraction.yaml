apiVersion: flow.aiwg.io/v1
kind: FlowCapability
metadata:
  name: civic-vote-ledger-extraction
  namespace: civic-action
  labels: {category: civic-meetings, idempotent: "true"}
  annotations: {research: docs/research/control-source-matrix.md, boundary: extracted-nonofficial-record}
spec:
  description: Prepare a non-official vote ledger with exact cues, explicit unknowns, and no voice-only identity inference.
  version: 1.0.0
  inputs: [{name: transcript_packet, type: object, required: true}]
  outputs: [{name: vote_ledger, type: object}]
  agent: citation-editor
  idempotent: true
